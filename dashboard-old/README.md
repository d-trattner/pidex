# PIDEX Dashboard (Legacy) – Archiv

Dies ist das legacy Dashboard (`dashboard-old`) aus der Vor-Migration.

Es ist als Referenz erhalten worden und darf nicht als aktiver Standard-Pfad genutzt werden.

## Inhalt
- `public/index.html` alte Motion/Liquid-Glass-Monolith-UI
- `scripts/server.py` Legacy Python-API
- `scripts/ingest.py` Legacy-Ingest
- Alte DB: `data/pidex.sqlite` (falls vorhanden)

## Nutzung
- **Nur für Referenz/Replay/Verifizierung verwenden.**
- Aktive Entwicklung/Migration erfolgt im neuen `dashboard/`-Ordner (TanStack Start).

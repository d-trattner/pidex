---
description: Start a PIDEX pipeline run
argument-hint: "[task]"
---
Start a PIDEX pipeline for this task:

$ARGUMENTS

Load and follow the canonical PIDEX orchestrator skill at `<pidex-root>/skills/pidex/SKILL.md`. Use direct mode and the bundled pidex-* agents.

---
description: Shortcut for /pidex — start a PIDEX pipeline run
argument-hint: "[task]"
---
Start a PIDEX pipeline for this task:

$ARGUMENTS

This is the `/pd` shortcut alias for `/pidex`. Load and follow the canonical PIDEX orchestrator skill at `<pidex-root>/skills/pidex/SKILL.md`. Use direct mode and the bundled pidex-* agents.

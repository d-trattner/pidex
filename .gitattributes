# Keep generated TanStack route tree stable across Windows/Linux checkouts.
dashboard/app/routeTree.gen.ts text eol=lf

# Bash scripts must stay LF-only so Git Bash/MSYS can execute them on Windows.
*.sh text eol=lf
